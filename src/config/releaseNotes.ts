// Auto-updated by release scripts. Do not edit manually.
export const BUNDLED_RELEASE_NOTES = {
  "version": "5.1.18",
  "tagName": "v5.1.18",
  "title": "Release 5.1.18 (Build 34) #fcb91f9",
  "body": "## Summary\n- **Voice memos in task cards** — Preview and play voice memos directly from the task list with a visual waveform; unread memos are highlighted and auto-marked as listened when played.\n- **Task history** — Task detail screens now show previous tasks created at the same spot, making it easy to review past work and recurring issues.\n- **Improved shared task acknowledgments** — See exactly who has acknowledged a shared task with per-recipient status and faster, optimistic UI updates.\n- **Cleaner workspaces list** — New setting to hide \"Shared with me\" from the Workspaces list if you prefer a focused view.\n\n## Changes\n- **Voice memos** — Added inline playback preview in task cards; visual waveform bars; unread dot indicator; auto-mark-as-listened on play.\n- **Task history section** — Expandable list of prior tasks at the same spot; shows status, priority, creation date, and description; tap to open historical task details.\n- **Shared task acknowledgments** — Redesigned acknowledgment UI showing individual recipient names and their status; optimistic updates for immediate feedback; success toast confirmations.\n- **Settings** — Added \"Hide Shared with me\" toggle under a new Workspaces section; release notes now render as formatted Markdown with links and code styling; fixed settings switches remounting issue.\n- **Permissions** — Task assignment is now permission-gated; users without the `assign-tasks` permission will not see assignee pickers in create or edit flows.\n- **Appearance** — Updated default theme to a modern blue palette; refreshed splash icon; improved dark mode contrast.\n- **Performance** — Faster Google sign-in by removing unnecessary server auth code request; added detailed auth timing logs for debugging.\n- **Localization** — Added translations for task history, acknowledgment confirmations, and workspace settings (EN/ES).\n\n## Technical Notes\n- **Build**: Version 5.1.18, Build 34, Git hash `fcb91f9`.\n- **Dependencies**: Added `react-native-markdown-display` for rich release notes rendering.\n- **Permissions**: New `usePermissions()` / `usePermission()` hooks query `api.users.myPermissions` and gate UI features like task assignment.\n- **Firebase**: Fixed `setBackgroundMessageHandler` import path; added approval-related notification types to background handler.\n- **Release pipeline**: Makefile now validates `OPENROUTER_API_KEY`, `RELEASE_NOTES_SECRET`, and `CONVEX_URL` before starting a release; improved error messages in `publish-release-notes.mjs`.\n- **Approval status logic**: `computeApprovalStatusForTask` now returns `null` instead of `'pending'` when no approval instances exist, preventing misleading \"Waiting for approval\" labels on completed tasks.",
  "bodyByLanguage": {
    "en": "## Summary\n- **Voice memos in task cards** — Preview and play voice memos directly from the task list with a visual waveform; unread memos are highlighted and auto-marked as listened when played.\n- **Task history** — Task detail screens now show previous tasks created at the same spot, making it easy to review past work and recurring issues.\n- **Improved shared task acknowledgments** — See exactly who has acknowledged a shared task with per-recipient status and faster, optimistic UI updates.\n- **Cleaner workspaces list** — New setting to hide \"Shared with me\" from the Workspaces list if you prefer a focused view.\n\n## Changes\n- **Voice memos** — Added inline playback preview in task cards; visual waveform bars; unread dot indicator; auto-mark-as-listened on play.\n- **Task history section** — Expandable list of prior tasks at the same spot; shows status, priority, creation date, and description; tap to open historical task details.\n- **Shared task acknowledgments** — Redesigned acknowledgment UI showing individual recipient names and their status; optimistic updates for immediate feedback; success toast confirmations.\n- **Settings** — Added \"Hide Shared with me\" toggle under a new Workspaces section; release notes now render as formatted Markdown with links and code styling; fixed settings switches remounting issue.\n- **Permissions** — Task assignment is now permission-gated; users without the `assign-tasks` permission will not see assignee pickers in create or edit flows.\n- **Appearance** — Updated default theme to a modern blue palette; refreshed splash icon; improved dark mode contrast.\n- **Performance** — Faster Google sign-in by removing unnecessary server auth code request; added detailed auth timing logs for debugging.\n- **Localization** — Added translations for task history, acknowledgment confirmations, and workspace settings (EN/ES).\n\n## Technical Notes\n- **Build**: Version 5.1.18, Build 34, Git hash `fcb91f9`.\n- **Dependencies**: Added `react-native-markdown-display` for rich release notes rendering.\n- **Permissions**: New `usePermissions()` / `usePermission()` hooks query `api.users.myPermissions` and gate UI features like task assignment.\n- **Firebase**: Fixed `setBackgroundMessageHandler` import path; added approval-related notification types to background handler.\n- **Release pipeline**: Makefile now validates `OPENROUTER_API_KEY`, `RELEASE_NOTES_SECRET`, and `CONVEX_URL` before starting a release; improved error messages in `publish-release-notes.mjs`.\n- **Approval status logic**: `computeApprovalStatusForTask` now returns `null` instead of `'pending'` when no approval instances exist, preventing misleading \"Waiting for approval\" labels on completed tasks.",
    "es": "## Resumen\n- **Notas de voz en tarjetas de tareas**: obtenga una vista previa y reproduzca notas de voz directamente desde la lista de tareas con una forma de onda visual;\n- **Historial de tareas**: las pantallas de detalles de tareas ahora muestran tareas anteriores creadas en el mismo lugar, lo que facilita la revisión del trabajo anterior y los problemas recurrentes.\n- **Reconocimientos de tareas compartidas mejorados**: vea exactamente quién ha reconocido una tarea compartida con el estado por destinatario y actualizaciones de interfaz de usuario más rápidas y optimistas.\n- **Lista de espacios de trabajo más limpios**: nueva configuración para ocultar \"Compartido conmigo\" de la lista de espacios de trabajo si prefiere una vista enfocada.\n## Cambios\n- **Notas de voz**: vista previa de reproducción agregada en línea en las tarjetas de tareas;\n- **Sección de historial de tareas**: lista ampliable de tareas anteriores en el mismo lugar;\n- **Reconocimientos de tareas compartidas**: interfaz de usuario de reconocimiento rediseñada que muestra los nombres de los destinatarios individuales y su estado;\n- **Configuración**: se agregó la opción \"Ocultar compartido conmigo\" en una nueva sección de Espacios de trabajo;\n- **Permisos**: la asignación de tareas ahora está limitada por permisos;\n- **Apariencia** — Tema predeterminado actualizado a una paleta azul moderna;\n- **Rendimiento**: inicio de sesión de Google más rápido al eliminar solicitudes innecesarias de código de autenticación del servidor;\n- **Localización**: se agregaron traducciones para el historial de tareas, confirmaciones de reconocimiento y configuración del espacio de trabajo (EN/ES).\n## Notas técnicas\n- **Compilación**: Versión 5.1.18, compilación 34, hash de Git `fcb91f9`.\n- **Dependencias**: se agregó `react-native-markdown-display` para una representación enriquecida de las notas de la versión.\n- **Permisos**: Nuevos enlaces `usePermissions()`/`usePermission()` que consultan `api.users.myPermissions` y funciones de interfaz de usuario de puerta como asignación de tareas.\n- **Firebase**: Se corrigió la ruta de importación `setBackgroundMessageHandler`;\n- **Canalización de lanzamiento**: Makefile ahora valida `OPENROUTER_API_KEY`, `RELEASE_NOTES_SECRET` y `CONVEX_URL` antes de iniciar un lanzamiento;\n- **Lógica del estado de aprobación**: `computeApprovalStatusForTask` ahora devuelve `null` en lugar de `'pendiente'` cuando no existen instancias de aprobación, lo que evita etiquetas engañosas de \"Esperando aprobación\" en las tareas completadas."
  },
  "buildNumber": 34,
  "gitHash": "fcb91f9"
} as const;
