// Auto-updated by release scripts. Do not edit manually.
export const BUNDLED_RELEASE_NOTES = {
  "version": "5.1.23",
  "tagName": "v5.1.23",
  "title": "Release 5.1.23 (Build 39) #89fc4e4",
  "body": "## Summary\n\n- **Instant task lists** — Cold-start performance improved dramatically with immediate hydration from local SQLite cache; task counts in sidebars and status pills now reflect accurate server-side totals rather than stale estimates\n- **Powerful task search** — Find tasks quickly by title, description, spot, status, assignee names, tags, or enter `#` followed by a task ID for direct lookup\n- **Rich approval details** — Team-based approvals now display individual member status, response times, and comments with progress badges (e.g., \"2/3\"); added support for \"Skipped\" approval statuses\n- **Reliable finished tasks** — Fixed the \"Show Finished Tasks\" toggle to properly load completed task history via lazy loading when enabled; preference persists across sessions and syncs to server\n\n## Changes\n\n### Task Loading & Offline Support\n- Task lists now load instantly from local cache without waiting for network round-trips, with full offline support displaying last-synced counts\n- Fixed inaccurate task counts (e.g., \"stuck at 34\") in navigation sidebars and workspace screens\n- Added animated loading indicators for synchronization and pagination states\n- Enhanced multi-workspace offline storage with improved data isolation\n\n### Task Search & Filtering  \n- Added global task search covering titles, descriptions, spots, statuses, assignees, and tags\n- Added setting to include finished tasks in lists; completed tasks load correctly via lazy loading when enabled\n- Added empty state messaging when no statuses match the selected category filter\n- Added permission error messaging when attempting to change task status without authorization\n\n### Approval Workflows\n- Expanded approval cards to show individual team member details including response status, timestamps, and comments\n- Added \"Skipped\" status translations and visual indicators for bypassed approval steps\n- Improved accuracy when the same approver appears in multiple approval steps\n- Added progress summaries (e.g., \"2/3\") to multi-step approvals\n\n### Settings & Preferences\n- New toggle in Settings to show or hide finished tasks across task lists\n- Preference persists locally and synchronizes with server preferences\n\n## Technical Notes\n\n- **Build:** 39 (`89fc4e4`)\n- **Database:** Schema upgraded from v5 → v8 with incremental migrations; new `task_cache` table with composite keys, workspace junction tables, and full-text search indexing\n- **Architecture:** Consolidated four competing read paths (in-memory reactive, indexed, SQLite list, SQLite archive) into unified model: windowed live query + server rollups with SQLite as offline cache\n- **Tooling:** Added `i18n-check.ts` validation scripts for translation completeness and hardcoded string detection\n- **Cleanup:** Removed ~300 lines of unreachable code from TaskContext including legacy archive paths and in-memory index reducers",
  "bodyByLanguage": {
    "en": "## Summary\n\n- **Instant task lists** — Cold-start performance improved dramatically with immediate hydration from local SQLite cache; task counts in sidebars and status pills now reflect accurate server-side totals rather than stale estimates\n- **Powerful task search** — Find tasks quickly by title, description, spot, status, assignee names, tags, or enter `#` followed by a task ID for direct lookup\n- **Rich approval details** — Team-based approvals now display individual member status, response times, and comments with progress badges (e.g., \"2/3\"); added support for \"Skipped\" approval statuses\n- **Reliable finished tasks** — Fixed the \"Show Finished Tasks\" toggle to properly load completed task history via lazy loading when enabled; preference persists across sessions and syncs to server\n\n## Changes\n\n### Task Loading & Offline Support\n- Task lists now load instantly from local cache without waiting for network round-trips, with full offline support displaying last-synced counts\n- Fixed inaccurate task counts (e.g., \"stuck at 34\") in navigation sidebars and workspace screens\n- Added animated loading indicators for synchronization and pagination states\n- Enhanced multi-workspace offline storage with improved data isolation\n\n### Task Search & Filtering  \n- Added global task search covering titles, descriptions, spots, statuses, assignees, and tags\n- Added setting to include finished tasks in lists; completed tasks load correctly via lazy loading when enabled\n- Added empty state messaging when no statuses match the selected category filter\n- Added permission error messaging when attempting to change task status without authorization\n\n### Approval Workflows\n- Expanded approval cards to show individual team member details including response status, timestamps, and comments\n- Added \"Skipped\" status translations and visual indicators for bypassed approval steps\n- Improved accuracy when the same approver appears in multiple approval steps\n- Added progress summaries (e.g., \"2/3\") to multi-step approvals\n\n### Settings & Preferences\n- New toggle in Settings to show or hide finished tasks across task lists\n- Preference persists locally and synchronizes with server preferences\n\n## Technical Notes\n\n- **Build:** 39 (`89fc4e4`)\n- **Database:** Schema upgraded from v5 → v8 with incremental migrations; new `task_cache` table with composite keys, workspace junction tables, and full-text search indexing\n- **Architecture:** Consolidated four competing read paths (in-memory reactive, indexed, SQLite list, SQLite archive) into unified model: windowed live query + server rollups with SQLite as offline cache\n- **Tooling:** Added `i18n-check.ts` validation scripts for translation completeness and hardcoded string detection\n- **Cleanup:** Removed ~300 lines of unreachable code from TaskContext including legacy archive paths and in-memory index reducers",
    "es": "## Resumen\n- **Listas de tareas instantáneas**: el rendimiento del arranque en frío mejoró drásticamente con la hidratación inmediata desde la caché SQLite local;\n- **Potente búsqueda de tareas**: encuentre tareas rápidamente por título, descripción, lugar, estado, nombres de asignados, etiquetas o ingrese `#` seguido de un ID de tarea para una búsqueda directa.\n- **Detalles de aprobación enriquecidos**: las aprobaciones basadas en equipos ahora muestran el estado de los miembros individuales, los tiempos de respuesta y los comentarios con insignias de progreso (por ejemplo, \"2/3\");\n- **Tareas terminadas confiables**: se corrigió la opción \"Mostrar tareas terminadas\" para cargar correctamente el historial de tareas completadas mediante carga diferida cuando está habilitado;\n## Cambios\n### Carga de tareas y soporte sin conexión\n- Las listas de tareas ahora se cargan instantáneamente desde la caché local sin tener que esperar a que la red vaya de ida y vuelta, con soporte completo sin conexión que muestra los últimos recuentos sincronizados.\n- Se corrigieron recuentos de tareas inexactos (por ejemplo, \"atascado en 34\") en las barras laterales de navegación y pantallas del espacio de trabajo\n- Se agregaron indicadores de carga animados para estados de sincronización y paginación.\n- Almacenamiento fuera de línea mejorado en múltiples espacios de trabajo con aislamiento de datos mejorado\n### Búsqueda y filtrado de tareas\n- Se agregó búsqueda de tareas global que cubre títulos, descripciones, lugares, estados, asignados y etiquetas.\n- Configuración agregada para incluir tareas terminadas en listas;\n- Se agregó mensaje de estado vacío cuando ningún estado coincide con el filtro de categoría seleccionado\n- Se agregaron mensajes de error de permiso al intentar cambiar el estado de la tarea sin autorización.\n### Flujos de trabajo de aprobación\n- Tarjetas de aprobación ampliadas para mostrar detalles individuales de los miembros del equipo, incluido el estado de la respuesta, marcas de tiempo y comentarios.\n- Se agregaron traducciones de estado \"Omitido\" e indicadores visuales para pasos de aprobación omitidos\n- Precisión mejorada cuando el mismo aprobador aparece en múltiples pasos de aprobación\n- Se agregaron resúmenes de progreso (por ejemplo, \"2/3\") a las aprobaciones de varios pasos\n### Configuración y preferencias\n- Nueva opción en Configuración para mostrar u ocultar tareas terminadas en las listas de tareas\n- La preferencia persiste localmente y se sincroniza con las preferencias del servidor.\n## Notas técnicas\n- **Construcción:** 39 (`89fc4e4`)\n- **Base de datos:** Esquema actualizado desde v5 → v8 con migraciones incrementales;\n- **Arquitectura:** Se consolidaron cuatro rutas de lectura competitivas (reactiva en memoria, indexada, lista SQLite, archivo SQLite) en un modelo unificado: consulta en vivo en ventana + resúmenes de servidor con SQLite como caché sin conexión\n- **Herramientas:** Se agregaron scripts de validación `i18n-check.ts` para verificar la integridad de la traducción y la detección de cadenas codificadas\n- **Limpieza:** Se eliminaron ~300 líneas de código inalcanzable de TaskContext, incluidas rutas de archivo heredadas y reductores de índice en memoria."
  },
  "buildNumber": 39,
  "gitHash": "89fc4e4"
} as const;
